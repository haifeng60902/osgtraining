Import('env')

#--------------------------------------#

target = 'Test1'

libs = [
	#'dx11backend',
	'renderApi',
	'LogInfo',
	'User32',
	'lua_static'
	]

not_linked_dependencies = [
#	'dx11backend',
#	'renderApi'
	]
	 
external_libs = ['dx11']

#--------------------------------------#

sources=env.Glob("*.cpp")
headers=env.Glob("*.h")

#--------------------------------------#

env.edProgram(
	target=target,
	source=sources,
	header=headers,
#	resource = ['Editors/Viewer.rc'],
	external_libs=external_libs,
	libs=libs,
	not_linked_dependencies=not_linked_dependencies,

    CPPPATH = [
	#	env['TOP_DIR']+'src/render/dx11backend',
		env['TOP_DIR']+'src/render/renderApi',
		env['TOP_DIR']+'src/lua/src'  
        ],
)
